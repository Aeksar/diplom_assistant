services:
  qdrant:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - diplom_assistant_data:/qdrant/storage:z
    networks:
      - my_network

volumes:
  diplom_assistant_data:

networks:
  my_network:
    driver: bridge